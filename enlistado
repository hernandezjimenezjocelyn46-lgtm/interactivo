<html>
    <head>
<style>
    #tabla th, tr, td{
        border: 1px solid black;
    }
</style>
    </head>
    
    <body>
<?php
$conexion = mysqli_connect("sql113.infinityfree.com", "BDJqeOOwjW7A", "if0_40528729_interactivo");
if(!$conexion) {
echo "Error: No se pudo conectar a MySQL.";
echo "errno de depuración: ", mysqli_connect_errno();
echo "error de depuración: ", mysqli_connect_error();
exit;
}
$resultado = mysqli_query($conexion, "SELECT * FROM visita");

?>

<table id="tabla">
    
    <tr>
        <th>Id</th>
        <th>Nombre</th>
        <th>Comentario</th>
    </tr>
    <?php
    while ($fila = $resultado ->fetch_assoc()){
    $id = $fila['id'];
    $nombre = $fila['Nombre'];
    $apellido = $fila['comentario'];
    echo "<tr>";
    echo "<td>";
    echo $fila['id'];
    echo "</td>";
    echo "<td>";
    echo $fila['Nombre'];
    echo "</td>";
    echo "<td>";
    echo $fila['comentario'];
    echo "</td>";
    echo "</tr>";
    };
    
    mysqli_close($conexion);
    ?> 
    
    </body>
    </html>